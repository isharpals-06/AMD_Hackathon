apiVersion: 1

providers:
  - name: 'AMD Router Dashboards'
    orgId: 1
    folder: 'AMD Router'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
