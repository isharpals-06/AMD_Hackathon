import { KeyRound, LayoutPanelTop, RotateCcw, Shield, Sparkles } from 'lucide-react'
import { useApp } from '../context/AppContext'
import { Button, Page, Panel } from '../components/UI'
export default function SettingsPage(){const {apiKey,setApiKey,compact,setCompact,reducedMotion,setReducedMotion,clearActivity}=useApp();return <Page eyebrow="PREFERENCES" title="Frontend settings" description="Configure browser-session API access, motion and layout without changing backend code."><div className="settings-grid"><Panel><div className="setting-head"><div className="setting-icon"><KeyRound/></div><div><h3>API key</h3><p>Stored in sessionStorage and sent as X-API-Key. It is cleared when the browser session ends.</p></div></div><input className="setting-input" type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder="Optional X-API-Key"/></Panel><Panel><div className="setting-row"><div className="setting-head"><div className="setting-icon"><LayoutPanelTop/></div><div><h3>Compact layout</h3><p>Reduce spacing density across dashboard pages.</p></div></div><button className={`toggle ${compact?'on':''}`} onClick={()=>setCompact(!compact)}><span/></button></div></Panel><Panel><div className="setting-row"><div className="setting-head"><div className="setting-icon"><Sparkles/></div><div><h3>Reduced motion</h3><p>Disable non-essential transitions and continuous routing effects.</p></div></div><button className={`toggle ${reducedMotion?'on':''}`} onClick={()=>setReducedMotion(!reducedMotion)}><span/></button></div></Panel><Panel className="danger-zone"><div className="setting-head"><div className="setting-icon"><RotateCcw/></div><div><h3>Clear session activity</h3><p>Remove locally stored playground requests. Backend records are unaffected.</p></div></div><Button variant="danger" onClick={clearActivity}>Clear session data</Button></Panel><Panel className="settings-note"><Shield/><div><h3>Security note</h3><p>No backend secrets are bundled into the frontend. Configure the API base URL through <code>VITE_API_BASE_URL</code> and provide optional API keys only at runtime.</p></div></Panel></div></Page>}
