import { BrainCircuit, Cloud, Database, GitBranch, Search, ShieldCheck } from 'lucide-react'
import RouterFlow from '../components/RouterFlow'
import { Page, Panel } from '../components/UI'
export default function RouterPage(){return <Page eyebrow="LIVE ROUTER" title="Conditional routing visualizer" description="A technically accurate view of the classifier cascade, task policy and primary/fallback execution path."><Panel glow className="router-stage"><div className="stage-grid"/><RouterFlow active="complete"/></Panel><div className="feature-grid compact-grid">{[[BrainCircuit,'Tier 1 · Router SLM','A fine-tuned compact model attempts fast intent classification first.'],[Search,'Tier 2 · Vector Search','ChromaDB retrieves semantically similar labelled examples when Tier 1 is uncertain.'],[ShieldCheck,'Tier 3 · Regex Fallback','Word-boundary rules guarantee a valid route when upstream classification fails.'],[GitBranch,'Task Policy','External YAML maps coding, mathematics, research and casual prompts to model chains.'],[Cloud,'Primary + Fallback','The executor attempts the preferred model, then automatically cascades on provider failure.'],[Database,'Metrics Persistence','SQLite WAL records requests, tokens, latency, status, model and cost without blocking concurrent writes.']].map(([Icon,t,d])=><Panel key={t} className="feature-card"><div className="feature-icon"><Icon/></div><h3>{t}</h3><p>{d}</p></Panel>)}</div></Page>}
